import { NumericFormControlDirective } from './numericFormControl.directive';

describe('NumericFormControlDirective', () => {
	it('should create an instance', () => {
		const directive = new NumericFormControlDirective();
		expect(directive).toBeTruthy();
	});
});
